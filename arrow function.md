```js
let arr = [1, 2, 3, 4, 5]
console.log(
  arr.filter(e =>{
    return e>0
  })
  .map(e=>{
    return e*2 
  })
)
```
```js 
const arr = [1, 2, 3, 4, 5];
console.log(
  arr.filter(e => e > 0)
    .map(e => e * 2),
);
```
